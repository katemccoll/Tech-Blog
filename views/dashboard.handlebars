<div class="row">
    {{#if blogs.length}}
        {{#each blogs as |blog|}}

        {{/each}}

    {{else}}
    {{/if}}
    <div>
        <button class="btn btn-primary" type="submit">+ New Post</button>
    </div>
</div>

<script src="js/dashboard.js"></script>